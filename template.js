const fast = "jan";

const last = "pakhi";

const name = fast + " " + last;
console.log(name);

const math = `Sum is ${fast} ${last}`;
console.log(math);

const email = `name : khattas
hamilll hattas`;

console.log(email);
